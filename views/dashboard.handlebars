
{{#if posts}}
<ol>
{{#each posts}}
    <div id="{{this.id}}">
<li>
    <h2>{{this.title}}</h2>
    <a href="/posts/{{this.id}}">Link to post</a>
    <p>{{this.content}}</p>
    <button>Edit Post</button>
</li>
    </div>
{{/each}}

</ol>
{{/if}}